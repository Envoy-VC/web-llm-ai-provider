{
	"name": "webllm-ai-provider",
	"private": true,
	"devDependencies": {
		"@commitlint/cli": "^19.5.0",
		"@commitlint/config-conventional": "^19.5.0",
		"husky": "^9.1.6",
		"lerna": "^8.1.9"
	},
	"lint-staged": {
		"*": "prettier -w --ignore-unknown"
	},
	"scripts": {
		"commitlint": "commitlint --edit",
		"prepare": "husky",
		"build": "lerna run build",
		"test": "lerna run test"
	}
}
